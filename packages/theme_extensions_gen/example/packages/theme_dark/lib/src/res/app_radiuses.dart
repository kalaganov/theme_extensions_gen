import 'package:flutter/material.dart';

const _borderRadius24 = BorderRadius.all(Radius.circular(24));
const _borderRadius16 = BorderRadius.all(Radius.circular(16));
const _borderRadius32 = BorderRadius.all(Radius.circular(32));

const buttonBorderRadius = _borderRadius24;
const cardBorderRadius = _borderRadius32;
const chipBorderRadius = _borderRadius16;
